{
  "name": "auto-office",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n back,front -c cyan,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
